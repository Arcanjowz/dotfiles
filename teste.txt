cd ~/sua/pasta          # entra na pasta
git init                # inicia o git
git add .               # marca tudo
git commit -m "msg"     # tira a foto
gh repo create NOME --private --source=. --remote=origin --push  # cria e envia


git add .               # marca o que mudou
git commit -m "msg"     # tira nova foto
git push                # envia